# 設計書 ― 琴平町 発信ダッシュボード（kotohira-news）

- 版: v2.1 / 2026-07-09
- 対象: Phase 0〜3（一部着手）
- 表示部の正本: `index.html`（2026-07-09 正本化・7タブ＋ハッシュルーティング＋詳細モーダル＋カレンダー表示。旧4タブ版は `index-legacy.html`）

> v1.0→v2.0 の主な変更: データ供給が **2系統**（RSS由来の `data.js` ＋ Notion 3DB由来の `notion-data.js`）になった／表示部を4タブから **5タブ（ホーム・ニュース・イベント・キャンペーン・団体）** に再設計し、イベント・キャンペーンをカレンダーで見せるようにした。
> v2.0→v2.1 の主な変更: 試作 `index-redesign3.html` を `index.html` へ **正本化**（2026-07-09。旧4タブ版は `index-legacy.html` に退避）。🧭体験・滞在（Notion 体験・滞在DB接続待ち）と🔗リンク（`links-data.js` のリンク集）を追加し **7タブ化**。

---

## 1. アーキテクチャ

データ供給は「1ファイルの書き手は1人」の原則で **2系統** に分かれる。

```
系統A（団体のSNS発信）
[ registry.json ]    団体×媒体の元データ（人が編集して育てる）
        │  ① 読み込み
        ▼
[ /kotohira-news スキル (Claude Code) ]
        │  ② note等のRSSを WebFetch で取得（最新最大10件）
        │  ③ items / instagram / x を組み立て
        ▼  ④ 上書き保存
[ data.js ]          window.DASHBOARD_DATA（団体・SNS更新）

系統B（町のニュース／イベント／キャンペーン）
[ Notion 3DB ]       ニュースDB／イベントDB／キャンペーンDB
        │  ① 読み取り専用トークンでNotion API
        ▼
[ scripts/fetch_notion.py ]  Python標準ライブラリのみ・pip依存なし
        │  ② サムネ署名URLを assets/thumbs/ に保存し相対パス化
        ▼  ③ 上書き保存
[ notion-data.js ]   window.NOTION_DATA（news / events / campaigns）

              data.js ＋ notion-data.js
                       │  <script src> で両方読み込み
                       ▼
[ index.html ]   表示部（固定。7タブUIで描画）
                       │
                       ▼
       ブラウザ（ローカル / 将来は GitHub Pages）
```

**設計原則**: ロジック（表示HTML・スキル・スクリプト）とデータ（registry.json / data.js / notion-data.js）を分離。データ追加・更新でロジックを触らない。`data.js` と `notion-data.js` はそれぞれ生成元が1つに固定され、混線しない。

> 補足: NotionニュースDBへの収集の実体は別リポジトリの **konpie-bot**（Railway常駐・30分毎にRSS/スクレイプ→Discord→NotionニュースDB自動登録）。本リポジトリの `fetch_notion.py` は、そうして溜まったNotion 3DBを **読み取るだけ**（書き込み権限は持たせない＝最小権限）。

## 2. ディレクトリ構成

```
C:\Users\user\kotohira-news\
├── index.html            表示部（現行の正本＝旧 index-redesign3.html を 2026-07-09 に正本化。7タブ版）
├── index-legacy.html     旧・表示部（4タブ版。正本化前の index.html を退避）
├── index-redesign.html   再設計 試作1（履歴）
├── index-redesign2.html  再設計 試作2（履歴）
├── data.js               系統Aの表示データ（/kotohira-news が再生成。手編集しない）
├── notion-data.js        系統Bの表示データ（fetch_notion.py が再生成。手編集しない）
├── registry.json         系統Aの元データ（人が編集する正本）
├── scripts/
│   ├── fetch_notion.py   Notion 3DB → notion-data.js 生成（pip依存なし）
│   └── README.md         セットアップ・環境変数・運用の手順
├── assets/thumbs/        サムネ画像のローカル保存（署名URL失効対策）
├── .env / .env.example   Notionトークン等（.env は .gitignore 済み）
├── README.md
├── .gitignore
└── docs/
    ├── 要件定義書.md
    ├── 設計書.md          ← このファイル
    ├── ロードマップ.md
    ├── 運用手順.md
    ├── 参考サイト調査.md   （再設計のインプット：他自治体・地域サイト10件のUI調査）
    └── 開発ダッシュボード.html
```

※ スキル本体はグローバル: `C:\Users\user\.claude\skills\kotohira-news\SKILL.md`（リポジトリには含めない）。
※ 試作 `index-redesign*.html` は表示部の検討過程（履歴）。2026-07-09 に `index-redesign3.html` を `index.html` として正本化済み（旧4タブ版は `index-legacy.html`）。

## 3. データモデル

### 3.1 registry.json（元データ）

```jsonc
{
  "project": "kotohira-news",
  "updated": "YYYY-MM-DD",
  "orgs": [
    {
      "name": "団体名",
      "summary": "概要",
      "channels": [
        { "type": "note", "url": "...", "rss": "https://note.com/{id}/rss",
          "auto": true, "verified": true, "note": "備考" },
        { "type": "instagram", "handle": "...", "url": "...",
          "auto": false, "verified": false }
      ]
    }
  ],
  "todo_orgs": ["未登録団体メモ"]
}
```

- `type`: `note` / `instagram` / `x` / `website` / `youtube` / `ameblo`
- `auto`: RSSで自動取得できるか。`verified`: URL/RSSを実取得で確認済みか。

### 3.2 data.js（表示データ・自動生成）

```jsonc
window.DASHBOARD_DATA = {
  "title": "琴平町 発信ダッシュボード",
  "updated": "YYYY-MM-DD",
  "orgs": [
    {
      "id": "matatabi",                 // 安定キー（英数字）
      "name": "...", "summary": "...",
      "links": { "note":"", "instagram":"", "x":"", "website":"", "youtube":"", "ameblo":"" },
      "items": [                        // RSS由来。新しい順・最大10
        { "source":"note", "date":"YYYY-MM-DD", "title":"...", "url":"...", "thumbnail":"(任意)" }
      ],
      "instagram": [ "投稿URL", ... 最大2 ],   // 手動登録→埋め込み
      "x":         [ "投稿URL", ... 最大2 ]     // 手動登録→埋め込み
    }
  ]
};
```

- **団体タブ（ドリルダウン）** = 団体の `items` を表示（更新／Instagram／X サブタブ）。

### 3.3 notion-data.js（表示データ・自動生成 / 系統B）

`scripts/fetch_notion.py` が Notion 3DB から生成。スキーマの正は収集側 konpie-bot のコード。

```jsonc
window.NOTION_DATA = {
  "fetched": "YYYY-MM-DD HH:MM",     // 生成時刻
  "news": [                          // ニュースDB由来
    { "id":"", "title":"", "url":"", "summary":"", "org":"発信元",
      "media":"新聞|テレビ|行政|Webメディア|note|Instagram|X|HP|その他",
      "date":"YYYY-MM-DD", "thumbnail":"assets/thumbs/xxx.png",
      "duplicate":false,             // 同じ出来事の別媒体報道の可能性
      "pickup":false,                // DAO定例で紹介予定（⭐定例ピックアップ）
      "show":true }                  // false のみ非表示（未定義は表示＝前方互換）
  ],
  "events": [                        // イベントDB由来
    { "id":"", "title":"", "url":"", "start":"YYYY-MM-DDTHH:MM", "end":"",
      "status":"実施前|実施中|終了", "city":"", "place":"",
      "image":"assets/thumbs/xxx.jpg", "comment":"", "fee":"", "organizer":"" }
  ],
  "campaigns": [ /* イベントDBと同一スキーマ */ ]
};
```

- **ニュースタブ** = `news` を `date` 降順。`show!==false` のみ表示。`pickup` は⭐定例ピックアップとして上部にフィーチャーカードで別立て。
- **イベント／キャンペーンタブ** = `events` / `campaigns` を `status` で実施中・実施前（近い順）・終了に整理し、月グリッドのカレンダーとカードで表示。
- イベントDBとキャンペーンDBは **完全同一スキーマ**。

**Notionプロパティとの対応（ニュースDB）**:
- `show` = チェックボックス「**表示対象**」。`fetch_notion.py` が `show=True` のニュースのみ出力する（例: 47件中16件）。ダッシュボードでどのニュースを見せるかをNotion側で人が制御できる。
- `pickup` = チェックボックス「**定例で紹介**」。DAO定例で取り上げる記事。
- ⚠️ `duplicate`（重複の可能性）は **現在のニュースDBにプロパティが存在しない**ため、`fetch_notion.py` は常に `false` を出力する（コードは将来のプロパティ追加を見越して残置。表示側の重複バッジ・薄め表示も実装済みだが現状は発火しない）。

## 4. 画面構成（index.html）

上部固定バー＋pillタブ（7枚）。`location.hash`（`#home` `#news` `#events` `#campaigns` `#experiences` `#orgs` `#links`）でルーティングし、`hashchange` で再描画。

```
┌─ 上部固定バー（sticky）─────────────────────────────────┐
│  琴平町 発信ダッシュボード     最終更新 data.js / Notion時刻 │
│  [🏠 ホーム][📰 ニュース][📅 イベント][🎁 キャンペーン][🧭 体験・滞在][📋 団体][🔗 リンク] │
└──────────────────────────────────────────────────┘
  ・ホーム     : ダイジェスト。最新ニュース4件／直近イベント／実施中キャンペーンを
                 横スクロールのboxカードで。各セクションに「もっと見る →」。
  ・ニュース   : ①⭐定例ピックアップ（大きめフィーチャーカード）→ ②リスト → ③ボックス。
                 ※フィルタチップ・カード⇄リスト切替UIは意図的に持たない（後述の決定）。
  ・イベント   : Googleカレンダー風の月グリッド＋カード。カード/日クリックで詳細モーダル。
  ・キャンペーン: 同上（月グリッド＋カード＋詳細モーダル）。
  ・体験・滞在 : 体験プログラム／おてつたび／ワーキングホリデー／求人／滞在・宿泊を種別グループ＋
                 カードで表示（クリックで詳細モーダル＝イベントカードのDRY流用）。データは Notion
                 「体験・滞在DB」由来だが現状は接続待ち（EXPERIENCE_NOTION_DB_ID 未設定でプレースホルダ表示）。
  ・団体       : 団体ディレクトリ表（名前/概要/SNSリンク/更新件数）→ 行クリックで
                 団体詳細にドリルダウン（更新／Instagram／X サブタブ）。団体検索あり。
  ・リンク     : 役場・観光・交通などの公式サイトへのリンク集（`links-data.js` 由来・コード直書き運用）。
```

- **詳細モーダル**: イベント／キャンペーン／ホームのニュース以外のカードで共用（DRY）。Esc・オーバーレイクリックで閉じる。フォーカス管理あり。
- **アクセシビリティ**: 白文字バッジはコントラスト比AA(4.5:1)を満たす色に調整。行・boxは `tabindex`＋`role="button"`＋`focus-visible` アウトライン対応。
- 配色: 和モダン（`--accent:#c8472f` ほか）。初代 index.html のシンプルな見た目を土台に、構造だけ拡張。
- 媒体バッジ色（AA調整後）: note=#258475 / Instagram=#c13584 / X=#1d1d1f / HP=#3170eb / YouTube=#df3047 / Ameblo=#298542 / 新聞=#3a5a80 / テレビ=#a9631e / 行政=#458263 / Webメディア=#7c5cbf / その他=#7e756a。

### 4.1 表示形式の設計方針（参考サイト調査の反映）

`docs/参考サイト調査.md`（他地域サイト10件のUI調査）の総括を踏まえ、**情報の性質で表示形式を変える**方針を採用（城崎温泉の事例）。純ニュースは流し読みできるリスト、催し・お得情報・注目トピックは写真カードで見せる。
一方、海士町由来の「カード⇄リスト切替＋フィルタチップ」は**あえて実装しない**判断（小規模・件数が多くないため導線を増やすより素直な一覧を優先）。詳細は同調査の末尾「redesign3への反映」を参照。

## 5. 埋め込みの仕組み

- **Instagram**: `<blockquote class="instagram-media" data-instgrm-permalink="URL">` ＋ `//www.instagram.com/embed.js` → `window.instgrm.Embeds.process()`。
- **X**: `<blockquote class="twitter-tweet"><a href="URL"></a></blockquote>` ＋ `platform.twitter.com/widgets.js` → `window.twttr.widgets.load()`。
- タブ切替の都度、表示中タブの埋め込みのみ process/load（スクリプト未ロード時はリトライ）。

## 6. 更新フロー（運用）

**系統A（団体SNS更新 / data.js）**
1. `registry.json` に団体・媒体・（必要なら）IG/X投稿URLを追記。
2. Claude Code で `/kotohira-news` 実行 → `data.js` 再生成。

**系統B（ニュース／イベント／キャンペーン / notion-data.js）**
1. Notion 3DBが最新であること（収集は konpie-bot が常時実施）。
2. `python scripts/fetch_notion.py` を実行 → `notion-data.js` 再生成＋サムネを `assets/thumbs/` に保存。
   - 一部DBがコネクト漏れ等で404でも、成功したDBだけ更新し失敗DBは既存内容を温存。

**共通**
3. 表示部（`index.html`）を開く（将来は push で Pages 更新）。

## 7. 将来拡張

- **表示部の確定（完了）**: 2026-07-09 に `index-redesign3.html` を `index.html` へ正本化済み。旧4タブ版は `index-legacy.html` に退避。あわせて7タブ化（🧭体験・滞在＝Notion 体験・滞在DB接続待ち、🔗リンク＝`links-data.js`）。
- **Phase 2**: GitHub Pages 公開（リポジトリ直下を配信）。`.env` や画像の署名URL残骸を出さないこと。
- **Phase 3**: 定期実行で `data.js` / `notion-data.js` を自動更新。
  - `data.js`: GitHub Actions ではClaude WebFetch不可のため、RSS取得は標準的なfetch/パーサに置換。
  - `notion-data.js`: `fetch_notion.py` はpip依存なしなので cron/Actions に載せやすい（ただしNotionトークンをSecretで管理。konpie-bot側のトークンローテーションで401停止する点に注意）。
